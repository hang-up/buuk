/**
 * Cache here some heavy scripts for the app.
 *
 * @type {_}
 * @private
 */

window._ = require('lodash')

window.highlightjs = require('markdown-it-highlightjs')

window.jQuery = window.$ = require('jquery');

require('materialize-css/dist/js/materialize.min')
